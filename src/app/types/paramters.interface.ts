import { ICovrolinColors } from "./covrolinColor.interface";
import { IRooms } from "./rooms.interface";

export interface IParametrs {
    range: string[],
    colors: ICovrolinColors[],
    rooms: IRooms[],
  }
  